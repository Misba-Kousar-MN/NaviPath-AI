# Mode: 'mock' for instant offline demonstration with verified scenarios, or 'real' to hit FastAPI
VITE_API_MODE=mock

# Base URL for the FastAPI backend (when VITE_API_MODE=real)
# Default is http://localhost:8001 (or http://localhost:8000)
VITE_API_BASE_URL=http://localhost:8001
