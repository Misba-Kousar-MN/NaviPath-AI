# /config

Environment/config for the backend now lives at [`../backend/.env.example`](../backend/.env.example) (copy to `backend/.env`, never commit the real file — see [`../.gitignore`](../.gitignore)). This directory is kept for any future non-backend config placeholders (e.g. frontend/Voiceflow/Bhashini secrets, Make.com webhooks) once that layer is built.

**No secrets are committed to this repository.**
